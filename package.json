{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test -- -- --passWithNoTests"
  }
}